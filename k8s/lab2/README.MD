## Lab2

Viết deployment cho một app có image là `alpine:latest` chạy command `sleep infinity` ở namespace lab2.

Sau đó tạo service để expore cho app này cũng như app ở lab1. Kiểm tra chắc chắn rằng hai app có thể giao tiếp với nhau.
