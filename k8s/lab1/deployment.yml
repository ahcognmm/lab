apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  namespace: lab1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: app-container
        image: nginx
        env:
          # TODO: add here
